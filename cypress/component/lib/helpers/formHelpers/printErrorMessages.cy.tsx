describe('printErrorMessages', () => {
  //   const inputid = 'test-id';
  //   it('should display max length error before submit', () => {
  //     const errors = ['String must contain at most 500 character(s)'];
  //     // TODO: Change to expected error
  //     const expectedErrMsg = 'test';
  //     const expected = printErrorMessages(inputid, false, errors);
  //     cy.mount(expected);
  //     cy.get(`p#errorMessage-${inputid}`).should('have.text', expectedErrMsg);
  //   });
});
