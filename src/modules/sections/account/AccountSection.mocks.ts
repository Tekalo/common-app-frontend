const base = {
  matchesPaused: false,
};

const submitted = {
  matchesPaused: false,
};

const paused = {
  matchesPaused: true,
};

export const mockAccountSectionProps = {
  base,
  submitted,
  paused,
};
