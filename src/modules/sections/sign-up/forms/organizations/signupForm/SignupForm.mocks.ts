import { ISignupForm } from '@/sections/sign-up/forms/organizations/signupForm/SignupForm';

const base: ISignupForm = {
  handleSubmit: () => void {},
  previousForm: undefined,
};

export const mockSignupFormProps = {
  base,
};
