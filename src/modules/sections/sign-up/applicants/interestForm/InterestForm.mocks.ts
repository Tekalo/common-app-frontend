import { IInterestForm } from './InterestForm';

const base: IInterestForm = {
  handleSave: () => {},
  handleSubmit: () => {},
  savedForm: {},
};

export const mockInterestFormProps = {
  base,
};
