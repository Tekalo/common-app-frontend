import TableModal from './TableModal';
import { mockTableModalProps } from './TableModal.mocks';

export default { component: TableModal };

export const Default = {
  args: { ...mockTableModalProps.base },
};
