import CustomImage from './CustomImage';
import { mockCustomImageProps } from './CustomImage.mocks';

export default { component: CustomImage };

export const Default = {
  args: { ...mockCustomImageProps.base },
};
