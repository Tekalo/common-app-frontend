import BaseTemplate from './freeTextOther';
import { mockBaseTemplateProps } from './freeTextOther.mocks';

export default { component: BaseTemplate };

export const Default = {
  args: { ...mockBaseTemplateProps.base },
};
