import SingleSelect from './SingleSelect';
import { mockSingleSelectProps } from './SingleSelect.mocks';

export default { component: SingleSelect };

export const Default = {
  args: { ...mockSingleSelectProps.base },
};
