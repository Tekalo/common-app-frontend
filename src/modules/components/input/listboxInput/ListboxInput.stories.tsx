import ListboxInput from './ListboxInput';
import { mockListboxInputProps } from './ListboxInput.mocks';

export default { component: ListboxInput };

export const Default = {
  args: { ...mockListboxInputProps.base },
};
