import { IFreeTag } from './FreeTag';

const base: IFreeTag = {
  name: 'text-input',
  value: 'Text Input',
  setValue: () => {},
  onBlur: () => {},
};

export const mockFreeTagProps = {
  base,
};
