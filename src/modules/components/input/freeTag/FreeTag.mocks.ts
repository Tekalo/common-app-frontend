import { IFreeTag } from './FreeTag';

const base: IFreeTag = {
  name: 'text-input',
  value: ['Text Input'],
  setValue: () => void {},
  onBlur: () => void {},
};

export const mockFreeTagProps = {
  base,
};
