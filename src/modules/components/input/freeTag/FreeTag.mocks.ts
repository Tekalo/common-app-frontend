import { IFreeTag } from './FreeTag';

const base: IFreeTag = {
  name: 'text-input',
  value: ['Text Input'],
  setValue: () => null,
  onBlur: () => null,
};

export const mockFreeTagProps = {
  base,
};
