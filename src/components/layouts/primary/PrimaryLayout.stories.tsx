import PrimaryLayout from './PrimaryLayout';
import { mockPrimaryLayoutProps } from './PrimaryLayout.mocks';

export default { component: PrimaryLayout };

export const Default = {
  args: { ...mockPrimaryLayoutProps.base },
};
